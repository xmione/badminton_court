
# Skip Let's Encrypt for development
SKIP_LETS_ENCRYPT=y
# Skip ClamAV to reduce resource usage
SKIP_CLAMD=y

# Skip Let's Encrypt for development
SKIP_LETS_ENCRYPT=y

# Skip ClamAV to reduce resource usage
SKIP_CLAMD=y

# Skip Let's Encrypt for development
SKIP_LETS_ENCRYPT=y

# Skip ClamAV to reduce resource usage
SKIP_CLAMD=y

# Skip Let's Encrypt for development
SKIP_LETS_ENCRYPT=y

# Skip ClamAV to reduce resource usage
SKIP_CLAMD=y
