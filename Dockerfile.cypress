# Dockerfile.cypress
FROM cypress/included:12.17.4

# Set working directory
WORKDIR /e2e

# Remove any existing cypress.json file that might be in the base image
RUN rm -f cypress.json

# Copy Cypress configuration and tests
COPY cypress/ ./cypress/
COPY cypress.config.js ./

# Default command
CMD ["cypress", "run"]